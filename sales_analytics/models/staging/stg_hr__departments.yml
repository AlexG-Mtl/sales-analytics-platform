version: 2

models:
  - name: stg_hr__departments
    description: Cleaned HR department records. One row per department.

    columns:
      - name: department_id
        tests:
          - not_null
          - unique

      - name: department_name
        tests:
          - not_null

      - name: cost_center